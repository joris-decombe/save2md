{
  "private": true,
  "name": "save2md",
  "version": "1.0.0",
  "description": "Browser extension to save web pages as Markdown",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "screenshots": "node scripts/take-screenshots.mjs"
  },
  "devDependencies": {
    "jsdom": "^26.0.0",
    "playwright": "^1.50.0",
    "vitest": "^3.0.0"
  }
}
